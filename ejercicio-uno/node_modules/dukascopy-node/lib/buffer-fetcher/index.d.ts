/// <reference types="node" />
import { CacheManagerBase } from '../cache-manager';
import { NotifyFn, BufferFetcherInput, BufferObject } from './types';
export declare class BufferFetcher {
    batchSize: number;
    pauseBetweenBatchesMs: number;
    notifyOnItemFetchFn?: NotifyFn;
    fetcherFn: ((url: string) => Promise<Buffer>) | undefined;
    retryCount: number;
    pauseBetweenRetriesMs: number;
    cacheManager?: CacheManagerBase;
    constructor({ batchSize, pauseBetweenBatchesMs, notifyOnItemFetchFn, fetcherFn, retryCount, pauseBetweenRetriesMs, cacheManager }: BufferFetcherInput);
    private fetchBatch;
    fetch(urls: string[]): Promise<BufferObject[]>;
    private fetchBuffer;
}
