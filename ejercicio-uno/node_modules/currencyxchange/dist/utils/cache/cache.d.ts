import * as NodeCache from "node-cache";
declare class CacheService {
    cache: NodeCache;
    constructor(ttl: number);
    get(key: string, fetchData: Function): any;
    del(keys: string[]): void;
    flush(): void;
}
export default CacheService;
